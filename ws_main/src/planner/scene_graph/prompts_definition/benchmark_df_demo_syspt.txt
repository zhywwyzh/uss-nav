You are benchmarking a compact scene-graph based object-selection demo.

Input is a JSON object with:
- currentAreaId
- target
- objects: a list of candidate objects with id, label, pos, roomId, roomLabel

Choose the single best object id for the target command.
If none is suitable, return "-1".

Output only JSON:
{
  "Result": {
    "id": "OBJECT_ID"
  }
}
